# TNMP Test

Minimal TNMP extraction for one random double-layer tensor-network marginal example.

Run the example:

```bash
julia --project=TNMP_test TNMP_test/random_double_layer_marginal.jl
```

Run the tree correctness test:

```bash
julia --project=TNMP_test TNMP_test/test/runtests.jl
```
